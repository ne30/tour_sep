<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
        <img src="https://img.icons8.com/emoji/2x/waving-hand-light-skin-tone.png" alt="Welcome Image" width="30" height="24" class="d-inline-block align-text-top">
    </a>
    <%# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> %>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Book Tour", tours_path, class: "nav-link", method: :get %>
        </li>
        <li class="nav-item">
          <%= link_to 'My Tickets',  tickets_path, class: "nav-link", method: :get %>
        </li>
        <li class="nav-item">
          <%= @user.is_admin ? ( link_to 'See All Ticket',  all_tickets_path, class: "nav-link" ) : "" %>
        </li>
        <li class="nav-item">
          <%= @user.is_admin ? ( link_to 'Add Tour',  add_tour_path, class: "nav-link" ) : "" %>
        </li>
      </ul>
    </div>
    <div id="navbarNav">
        <ul class="navbar-nav justify-content-end collapse navbar-collapse">
            <li class="nav-item">
                <div class= "nav-link" data-toggle="tooltip" data-html="true" title= "User Id => <%= @user.id.to_s %> and gender => <%= @user.gender.eql?("m") ? "Male": "Female" %>" >
                    Welcome <%= @user.user_name %>
                </div>
            </li>
            <li class="nav-item">
            <%= @user.is_admin ? (button_to 'Log Out',  logout_admin_path, method: :post) : (button_to 'Log Out',  logout_path, method: :post) %>
            </li>
        </ul>
    </div> 
  </div>
</nav>